na podtawie podanych informacji znajdź w repo miejsce gdzie jest dokumentacja i dadaj poniższe dane 

Oto rozszerzona dokumentacja dla każdego z 11 testów statystycznych.

Możesz użyć tych opisów jako dokumentacji technicznej dla agenta implementującego lub jako treści pomocy i interpretacji dla użytkowników końcowych w Twojej aplikacji.

-----

## I. Testy do Porównywania Grup

Służą do sprawdzania, czy istnieją statystycznie istotne różnice w wartościach liczbowych między dwiema lub większą liczbą grup kategorycznych.

### 1\. Test t dla prób niezależnych (Independent T-test)

  * **Cel:** 🎯 Porównuje **średnie** dwóch **niezależnych** od siebie grup, aby stwierdzić, czy różnica między nimi jest statystycznie istotna.
  * **Scenariusz aplikacji:** 🧑‍🤝‍🧑 Użytkownik wybrał **1 kolumnę numeryczną** (np. 'Wynik') i **1 kolumnę kategoryczną** (np. 'Grupa'), która ma dokładnie **2 unikalne wartości** (np. 'A' i 'B').
  * **Kluczowe założenia (do sprawdzenia):**
    1.  **Normalność:** Dane w kolumnie numerycznej mają rozkład normalny (sprawdzone **Testem Shapiro-Wilka**, p \> 0.05).
    2.  **Homogeniczność wariancji:** Wariancje (zmienność) w obu grupach są równe (sprawdzone **Testem Levene'a**, p \> 0.05).
  * **Hipotezy (do raportu):**
      * **H0 (Hipoteza Zerowa):** Średnia wartość w grupie A jest równa średniej wartości w grupie B.
      * **H1 (Hipoteza Alternatywna):** Średnia wartość w grupie A jest różna od średniej wartości w grupie B.
  * **Implementacja (Python):** 🐍
    ```python
    # data1, data2 = grupy danych z Pandasa
    from scipy.stats import ttest_ind
    stat, p_value = ttest_ind(data1, data2, equal_var=True)
    ```

### 2\. Test t Welcha (Welch's T-test)

  * **Cel:** 🎯 Porównuje **średnie** dwóch **niezależnych** grup, gdy ich **wariancje są różne**. Jest to modyfikacja standardowego testu t.
  * **Scenariusz aplikacji:** 🧑‍🤝‍🧑 Użytkownik wybrał **1 kolumnę numeryczną** i **1 kolumnę kategoryczną** (z 2 grupami).
  * **Kluczowe założenia (do sprawdzenia):**
    1.  **Normalność:** Dane w kolumnie numerycznej mają rozkład normalny (Test Shapiro-Wilka, p \> 0.05).
    2.  **Brak homogeniczności wariancji:** Wariancje w obu grupach są **różne** (Test Levene'a, p \<= 0.05).
  * **Hipotezy (do raportu):**
      * **H0:** Średnia wartość w grupie A jest równa średniej wartości w grupie B.
      * **H1:** Średnia wartość w grupie A jest różna od średniej wartości w grupie B.
  * **Implementacja (Python):** 🐍
    ```python
    from scipy.stats import ttest_ind
    stat, p_value = ttest_ind(data1, data2, equal_var=False) # Kluczowa zmiana
    ```

### 3\. Test U Manna-Whitneya (Mann-Whitney U Test)

  * **Cel:** 🎯 **Nieparametryczny** odpowiednik testu t. Porównuje **mediany** (lub ogólnie rozkłady) dwóch **niezależnych** grup.
  * **Scenariusz aplikacji:** 🧑‍🤝‍🧑 Użytkownik wybrał **1 kolumnę numeryczną** i **1 kolumnę kategoryczną** (z 2 grupami).
  * **Kluczowe założenia (do sprawdzenia):**
    1.  **Brak normalności:** Dane w kolumnie numerycznej **nie mają** rozkładu normalnego (Test Shapiro-Wilka, p \<= 0.05).
  * **Hipotezy (do raportu):**
      * **H0:** Rozkłady wartości w obu grupach są takie same (lub: mediana w grupie A jest równa medianie w grupie B).
      * **H1:** Rozkłady wartości w obu grupach różnią się (lub: mediana w grupie A jest różna od mediany w grupie B).
  * **Implementacja (Python):** 🐍
    ```python
    from scipy.stats import mannwhitneyu
    stat, p_value = mannwhitneyu(data1, data2)
    ```

### 4\. Jednoczynnikowa ANOVA (One-Way ANOVA)

  * **Cel:** 🎯 Porównuje **średnie** w **trzech lub więcej** niezależnych grupach, aby sprawdzić, czy co najmniej jedna grupa różni się od pozostałych.
  * **Scenariusz aplikacji:** 👨‍👩‍👧‍👦 Użytkownik wybrał **1 kolumnę numeryczną** i **1 kolumnę kategoryczną**, która ma **3 lub więcej** unikalnych wartości (np. 'Grupa A', 'Grupa B', 'Grupa C').
  * **Kluczowe założenia (do sprawdzenia):**
    1.  **Normalność:** Dane w kolumnie numerycznej mają rozkład normalny (Test Shapiro-Wilka, p \> 0.05).
    2.  **Homogeniczność wariancji:** Wariancje we wszystkich grupach są równe (Test Levene'a, p \> 0.05).
  * **Hipotezy (do raportu):**
      * **H0:** Średnie wartości we wszystkich grupach są sobie równe.
      * **H1:** Co najmniej jedna grupa ma średnią wartość różną od pozostałych.
  * **Implementacja (Python):** 🐍
    ```python
    # groups_data = [data_A, data_B, data_C, ...]
    from scipy.stats import f_oneway
    stat, p_value = f_oneway(*groups_data)
    ```

### 5\. Test Kruskala-Wallisa (Kruskal-Wallis H Test)

  * **Cel:** 🎯 **Nieparametryczny** odpowiednik testu ANOVA. Porównuje **mediany** (lub rozkłady) w **trzech lub więcej** niezależnych grupach.
  * **Scenariusz aplikacji:** 👨‍👩‍👧‍👦 Użytkownik wybrał **1 kolumnę numeryczną** i **1 kolumnę kategoryczną** (z 3+ grupami).
  * **Kluczowe założenia (do sprawdzenia):**
    1.  **Brak normalności:** Dane w kolumnie numerycznej **nie mają** rozkładu normalnego (Test Shapiro-Wilka, p \<= 0.05) LUB
    2.  **Brak homogeniczności wariancji:** Wariancje w grupach są **różne** (Test Levene'a, p \<= 0.05).
  * **Hipotezy (do raportu):**
      * **H0:** Rozkłady wartości we wszystkich grupach są takie same (lub: mediany we wszystkich grupach są równe).
      * **H1:** Co najmniej jedna grupa ma rozkład wartości różny od pozostałych (lub: co najmniej jedna mediana jest różna).
  * **Implementacja (Python):** 🐍
    ```python
    # groups_data = [data_A, data_B, data_C, ...]
    from scipy.stats import kruskal
    stat, p_value = kruskal(*groups_data)
    ```

-----

## II. Testy Zależności (Korelacji i Asocjacji)

Służą do sprawdzania, czy dwie zmienne są ze sobą powiązane (czy zmiana w jednej wiąże się ze zmianą w drugiej).

### 6\. Współczynnik Korelacji Pearsona (r)

  * **Cel:** 📈 Mierzy siłę i kierunek **liniowej** zależności między **dwoma zmiennymi numerycznymi**.
  * **Scenariusz aplikacji:** 🔢 Użytkownik wybrał **2 kolumny numeryczne** (np. 'Wzrost' i 'Waga').
  * **Kluczowe założenia (do sprawdzenia):**
    1.  **Normalność:** Obie zmienne numeryczne mają rozkład normalny (Test Shapiro-Wilka, p \> 0.05 dla obu).
    2.  **Liniowość:** Zależność między zmiennymi jest (wizualnie) liniowa (trudne do automatyzacji, ale normalność jest silną wskazówką).
  * **Hipotezy (do raportu):**
      * **H0:** Nie ma liniowej korelacji między zmienną X a zmienną Y (współczynnik korelacji = 0).
      * **H1:** Istnieje liniowa korelacja między zmienną X a zmienną Y (współczynnik korelacji \!= 0).
  * **Implementacja (Python):** 🐍
    ```python
    # x, y = dwie kolumny danych z Pandasa
    from scipy.stats import pearsonr
    correlation_coefficient, p_value = pearsonr(x, y)
    ```

### 7\. Współczynnik Korelacji rang Spearmana (rho)

  * **Cel:** 📉 Mierzy siłę i kierunek **monotonicznej** zależności między dwoma zmiennymi (mogą być numeryczne lub porządkowe).
  * **Scenariusz aplikacji:** 🔢 Użytkownik wybrał **2 kolumny numeryczne**.
  * **Kluczowe założenia (do sprawdzenia):**
    1.  **Brak normalności:** Co najmniej jedna ze zmiennych **nie ma** rozkładu normalnego (Test Shapiro-Wilka, p \<= 0.05).
    2.  **Zależność nieliniowa:** Używany, gdy zależność nie jest liniowa, ale jest monotoniczna (jedna zmienna rośnie, gdy druga rośnie/maleje, ale niekoniecznie w stałym tempie).
  * **Hipotezy (do raportu):**
      * **H0:** Nie ma monotonicznej korelacji między zmienną X a zmienną Y.
      * **H1:** Istnieje monotoniczna korelacja między zmienną X a zmienną Y.
  * **Implementacja (Python):** 🐍
    ```python
    from scipy.stats import spearmanr
    correlation_coefficient, p_value = spearmanr(x, y)
    ```

### 8\. Test Chi-kwadrat (χ²) Niezależności

  * **Cel:** 📊 Sprawdza, czy istnieje **związek (asocjacja)** między **dwoma zmiennymi kategorycznymi**.
  * **Scenariusz aplikacji:** 🔠 Użytkownik wybrał **2 kolumny kategoryczne** (np. 'Płeć' i 'Wykształcenie').
  * **Kluczowe założenia (do sprawdzenia):**
    1.  **Wielkość próby:** Wartości oczekiwane w każdej komórce tabeli kontyngencji (krzyżowej) są odpowiednio duże (zwykle \> 5).
  * **Hipotezy (do raportu):**
      * **H0:** Zmienna A i zmienna B są od siebie **niezależne** (nie ma między nimi związku).
      * **H1:** Zmienna A i zmienna B są od siebie **zależne** (istnieje między nimi związek).
  * **Implementacja (Python):** 🐍
    ```python
    import pandas as pd
    from scipy.stats import chi2_contingency
    contingency_table = pd.crosstab(df['cat_var_1'], df['cat_var_2'])
    chi2_stat, p_value, dof, expected_freqs = chi2_contingency(contingency_table)
    ```

### 9\. Dokładny Test Fishera (Fisher's Exact Test)

  * **Cel:** 📊 Alternatywa dla testu Chi-kwadrat, używana gdy założenie o wielkości próby nie jest spełnione. Szczególnie dla małych próbek i tabel 2x2.
  * **Scenariusz aplikacji:** 🔠 Użytkownik wybrał **2 kolumny kategoryczne** (zwłaszcza 2x2, np. 'Palący: Tak/Nie' vs 'Choroba: Tak/Nie').
  * **Kluczowe założenia (do sprawdzenia):**
    1.  **Mała próba:** Używany, gdy test Chi-kwadrat jest nieodpowiedni, ponieważ **wartości oczekiwane w komórkach są zbyt małe** (np. \< 5).
  * **Hipotezy (do raportu):**
      * **H0:** Zmienna A i zmienna B są od siebie **niezależne**.
      * **H1:** Zmienna A i zmienna B są od siebie **zależne**.
  * **Implementacja (Python):** 🐍
    ```python
    from scipy.stats import fisher_exact
    # Działa tylko dla tabel 2x2
    odds_ratio, p_value = fisher_exact(table_2x2)
    ```

-----

## III. Testy dla Danych Sparowanych (Zależnych)

Służą do porównywania dwóch pomiarów wykonanych na tej samej grupie (np. "przed" i "po").

### 10\. Test t dla prób zależnych (Paired T-test)

  * **Cel:** 🔬 Porównuje **średnie** dwóch **powiązanych** ze sobą pomiarów (np. wynik tego samego pacjenta przed leczeniem i po leczeniu).
  * **Scenariusz aplikacji:** 🔁 Użytkownik wybrał **2 kolumny numeryczne** I dodatkowo zaznaczył opcję **"Dane są sparowane"**.
  * **Kluczowe założenia (do sprawdzenia):**
    1.  **Normalność różnic:** **Różnice** między parami pomiarów (np. `po - przed`) mają rozkład normalny (Test Shapiro-Wilka na kolumnie różnic, p \> 0.05).
  * **Hipotezy (do raportu):**
      * **H0:** Średnia różnica między pomiarem 1 a pomiarem 2 wynosi zero (nie ma zmiany).
      * **H1:** Średnia różnica między pomiarami jest różna od zera (nastąpiła zmiana).
  * **Implementacja (Python):** 🐍
    ```python
    # data_before, data_after = dwie sparowane kolumny
    from scipy.stats import ttest_rel
    stat, p_value = ttest_rel(data_before, data_after)
    ```

### 11\. Test rang Wilcoxona (Wilcoxon Signed-Rank Test)

  * **Cel:** 🔬 **Nieparametryczny** odpowiednik testu t dla prób zależnych. Porównuje **mediany** dwóch **powiązanych** pomiarów.
  * **Scenariusz aplikacji:** 🔁 Użytkownik wybrał **2 kolumny numeryczne** I zaznaczył opcję **"Dane są sparowane"**.
  * **Kluczowe założenia (do sprawdzenia):**
    1.  **Brak normalności różnic:** **Różnice** między parami pomiarów **nie mają** rozkładu normalnego (Test Shapiro-Wilka na kolumnie różnic, p \<= 0.05).
  * **Hipotezy (do raportu):**
      * **H0:** Mediana różnic między pomiarem 1 a pomiarem 2 wynosi zero (nie ma zmiany).
      * **H1:** Mediana różnic między pomiarami jest różna od zera (nastąpiła zmiana).
  * **Implementacja (Python):** 🐍
    ```python
    from scipy.stats import wilcoxon
    stat, p_value = wilcoxon(data_before, data_after)
    ```


Rozumiem. Chcesz włączyć **automatyczne generowanie wizualizacji i statystyk opisowych (deskryptywnych)** bezpośrednio po wyborze kolumn, jeszcze zanim użytkownik podejmie decyzję o wykonaniu właściwego testu statystycznego.

Poniższy plan rozszerza poprzednie kroki (zwłaszcza **Krok 5** i **Krok 6**) o moduł eksploracji danych.

---

## 🚀 Zaktualizowany Plan Implementacji: Włączanie Wizualizacji i Statystyk Opisowych

### Zmodyfikowany Przepływ Aplikacji:

1.  Wgranie i ładowanie danych.
2.  Wybór kolumn.
3.  **(NOWOŚĆ): Wyświetlenie statystyk opisowych i automatycznie wygenerowanych wykresów.**
4.  Sprawdzenie założeń testu i rekomendacja.
5.  Wykonanie testu statystycznego.

---

### Krok 5 (Zmieniony): UI do Wyboru Kolumn i Wyświetlania Danych Opisowych

**Cel:** Umożliwienie wyboru kolumn i natychmiastowe wygenerowanie statystyk deskryptywnych oraz wykresów.

| Czynność | Opis dla Agenta | Elementy Kodowania |
| :--- | :--- | :--- |
| **A. Wybór Kolumn (UI)** | Po wgraniu danych (Krok 4), wyświetl `st.multiselect` dla kolumn. Wybór ten wyzwala analizę deskryptywną. | `st.multiselect("Wybierz kolumny...", key='selected_columns', on_change=clear_analysis_results)` |
| **B. Wywołanie Analizy** | Gdy `len(selected_columns) > 0`, wywołaj nową funkcję, która obliczy statystyki opisowe i wygeneruje wykresy. | `if len(st.session_state.get('selected_columns', [])) > 0: display_exploratory_data(df, selected_columns)` |
| **C. Funkcja Deskryptywna (Backend)** | Stwórz funkcję `def get_descriptive_stats(df, col):`. Powinna ona zwracać słownik lub DataFrame z kluczowymi miarami. | Użyj **Pandas**: `df[col].describe()`. Dodatkowo oblicz modę (`df[col].mode()`), zakres interkwartylowy (IQR), oraz wariancję (`df[col].var()`). |
| **D. Zapis i Wyświetlanie** | Zapisz statystyki deskryptywne w `st.session_state` (np. `'descriptive_stats'`). Wyświetl je w formie czytelnej tabeli dla każdej wybranej kolumny. | `st.subheader("Statystyki Opisowe")`; `st.dataframe(...)` |

---

### Krok 6 (Nowy): Automatyczne Generowanie Wykresów

**Cel:** Automatyczne generowanie **najbardziej informatywnych wykresów** (wykresy rozkładu) w zależności od typu wybranej kolumny, aby użytkownik mógł wizualnie ocenić rozkład (kluczowe założenie testów parametrycznych).

| Typ Danych | Sugerowany Wykres | Cel | Implementacja (Biblioteki) |
| :--- | :--- | :--- | :--- |
| **Numeryczne** (pojedyncza kolumna) | **Histogram** (z funkcją gęstości/KDE) | Wizualizacja rozkładu danych i **normalności** (sprawdzenie, czy jest to dzwon). | `plotly.express` (`px.histogram`) LUB `seaborn` (`sns.histplot`) |
| **Numeryczne** (dla każdej grupy) | **Wykres pudełkowy (Box Plot)** | Porównanie median, kwartyli, zakresu oraz **odchyleń i symetrii** między grupami kategorycznymi. | `plotly.express` (`px.box`) LUB `seaborn` (`sns.boxplot`) |
| **Kategoryczne** (pojedyncza kolumna) | **Wykres słupkowy (Bar Chart)** | Pokazanie częstości występowania każdej kategorii. | `plotly.express` (`px.bar`) |
| **2 Numeryczne** (dla korelacji) | **Wykres punktowy (Scatter Plot)** | Wizualizacja **liniowości** lub **monotoniczności** zależności. | `plotly.express` (`px.scatter`) |

**Szczegółowy Opis dla Agenta (Logika generowania wykresów):**

1.  Stwórz funkcję `def generate_plots(df, selected_cols):`.
2.  **Logika:** Sprawdź, ile kolumn wybrano:
    * **Jedna kolumna numeryczna:** Wygeneruj **Histogram** dla tej kolumny.
    * **Jedna kolumna kategoryczna i jedna numeryczna:** Wygeneruj **Box Plot** (zmienna numeryczna na osi Y, kategoryczna na osi X). Użyj `plotly.express` dla interaktywności.
    * **Dwie kolumny numeryczne:** Wygeneruj **Scatter Plot**.
    * **Dwie kolumny kategoryczne:** Wygeneruj **Mozaikowy wykres słupkowy** (częstości).
3.  Wyświetl wygenerowane wykresy za pomocą `st.plotly_chart()` (wymaga `plotly` w `requirements.txt`).

---

### Krok 7 (Zmieniony): Sprawdzanie Założeń i Rekomendacja Testu

**Cel:** Uruchomienie logiki sprawdzania założeń *po* wyświetleniu wizualizacji, kończąc przyciskiem do wykonania testu.

| Czynność | Opis dla Agenta | Elementy Kodowania |
| :--- | :--- | :--- |
| **A. Wyświetlanie założeń** | Wyświetl podsumowanie sprawdzenia założeń (normalność, Levene'a) w `st.expander`, aby użytkownik mógł je przejrzeć. | Wykorzystaj słownik `'assumptions'` zapisany w stanie sesji. |
| **B. Rekomendacja Testu** | Wyświetl nazwę rekomendowanego testu (`'recommendation'`). | `st.success(...)` |
| **C. Oczekiwanie na Wykonanie (UI)** | **Usuń** automatyczne wykonywanie analizy. Wprowadź jeden wyraźny przycisk. | `st.button("Wykonaj Analizę Statystyczną i Raport", key='run_test_final', type="primary")` |
| **D. Flaga Wykonania** | Kliknięcie przycisku ustawia flagę w stanie sesji na `True`, co wyzwala Krok 8 (wcześniej Krok 9). | `if st.button(...): st.session_state['run_analysis_flag'] = True` |

---

### Krok 8: Router Funkcji Statystycznych i Raport

**Cel:** Ten krok pozostaje niezmieniony, ale jest wyzwalany przez **przygotowany przycisk** (Krok 7C), a nie automatycznie.

1.  Logika sprawdza flagę `'run_analysis_flag'`.
2.  Wywołuje router `run_statistical_test(df, cols, rec_key)`.
3.  Zapisuje wyniki do `'results'`.
4.  Wyświetla pełny raport (hipotezy, p-wartość, interpretacja).

---

### Podsumowanie Zmian dla Agenta

| Sekcja Planu | Zmiana | Kluczowe Wyzwanie |
| :--- | :--- | :--- |
| **Krok 5 (UI)** | Dodanie logiki wywołującej statystyki opisowe. | Wdrożenie logiki **Pandas** (`.describe()`, `.var()`, `.mode()`) i wyświetlanie ich w `st.dataframe`. |
| **Krok 6 (Nowy)** | Wprowadzenie modułu **Automatycznej Wizualizacji** (Histogramy, Box Ploty, Scatter Ploty). | Poprawne użycie `plotly.express` lub `seaborn` i **dynamiczne** generowanie wykresów w zależności od **typu i liczby** wybranych kolumn. |
| **Krok 7 (UI/Flow)** | Usunięcie automatycznego wykonania analizy na rzecz **jednego przycisku** do wygenerowania raportu po wizualnej ocenie danych. | Zapewnienie, że wszystkie dane opisowe i wykresy są widoczne *przed* tym przyciskiem. |